# 主机负载调查

利用主机监控指标观察负载情况与趋势，并采集主机详细信息，供后续分析负载升高的相关进程与原因。

## Language

**主机采集代理（agent）**：
部署在被调查主机上的采集角色，向 server 回报主机详细信息，不调用大模型。
_Avoid_: 主机诊断模型

**分析服务（server）**：
接收主机采集结果并负责后续分析的服务端角色。

**主机监控指标**：
反映主机负载情况及其随时间变化的观测数据。

**进程归属**：
进程与其所属容器、Kubernetes Pod 或业务之间的关联。无法识别的关联为未知，不据此认定该进程属于或不属于某个容器、Pod 或业务。

**只读采集**：
在资源预算内读取被调查主机和进程的状态，不修改被调查系统的配置、不控制业务进程、不附加调试或动态探针。允许受控采集开销、在指定存储目录写入自身数据、转存自身采集结果，以及管理代理自己创建的采集子进程。
_Avoid_: 零开销采集、整个 agent 零写入

**后台采样**：
不依赖调查请求触发、持续以低频进行的主机状态采样，用于提供近期观测记录。

**按需采样**：
收到调查请求后，在一个有限时间窗口内进行的主机状态采样。

**采集任务**：
server 发起的一次按需调查，包含采样要求、执行状态及对应的采集结果。
_Avoid_: 单个采样点

**采样窗口**：
一次按需采样所覆盖的持续时间。

**采样步长**：
同一次采样中，相邻两次计划采样之间的时间间隔。
_Avoid_: 采样窗口

**主机采集结果**：
通过只读采集获取、交给后续环节处理的主机详细信息。采集结果是分析依据，本身不等同于已经确认的负载升高原因。
_Avoid_: 根因结论

**调查场景**：
一次主机负载调查关注的资源问题类别，包括 CPU、I/O、内存和网络。场景用于界定需要回答的问题，不等同于已经确认的故障原因。

**调查快照（snapshot）**：
一次采样窗口内取得的主机调查证据集合，可以同时服务多个调查场景。窗口内观测具有各自的实际时间，不代表全主机在同一瞬间的原子状态。
_Avoid_: 单次瞬时读数、根因结论

**采集子进程**：
由主机采集代理创建、用于取得调查证据的进程，其启动、停止与回收由代理负责。它与被调查的业务进程具有不同的控制边界。

**结果转存**：
将已经生成的主机采集结果保存到调查方能够访问的远程位置，使调查方无需直接连接被调查主机即可取得证据。转存不代表已经分析或确认负载原因。
